#!/bin/bash
set -euo pipefail

source /etc/update-motd.d/colors.txt

# Count processes excluding header line
PROCESS_COUNT=$(ps -ef | tail -n +2 | wc -l)

echo -e " ${TITLE_COLOR}Processes...........:${NC} ${TEXT_COLOR}${PROCESS_COUNT}${DEFAULT_COLOR} running processes"

#!/bin/bash

source /etc/update-motd.d/colors.txt 2>/dev/null || true

if command -v toilet >/dev/null 2>&1; then
    echo -e "${DEFAULT_COLOR}"; toilet -d /etc/update-motd.d/ -f ivrit "you name"
else
    echo -e "${DEFAULT_COLOR:-}you name${NC:-}"
fi
echo ""
